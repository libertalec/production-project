import React from 'react';
import classes from './Counter.module.scss';

export const Countet = () => {
    return (
        <div >
           <button className={classes.btn}>Click!</button>
        </div>
    );
};